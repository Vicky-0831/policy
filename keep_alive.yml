name: Keep App Alive

on:
  schedule:
    # 每天北京时间早上 8 点到晚上 10 点，每小时访问一次
    - cron: '0 0-14 * * *'
  workflow_dispatch: 

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit App
        run: curl -s https://policy-search-vk.streamlit.app/ > /dev/null
